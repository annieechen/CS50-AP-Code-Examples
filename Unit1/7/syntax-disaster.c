/**
 * syntax-disaster.c
 * 
 * NOTE: this code is not meant to compile 
 * 
 * Can be used as a worksheet or in class exercise to fix the syntax errors from the top down
 */    
#include <cs50.h>
#include <stdio.h>

int main(void)
{
    printf(hello, world\n")
    
    cat = 5;
    
    while (cat = 5)
    {
        cat 3++;
    }
    
    string catSound;
    
    "meow" = catSound;
    
    for (int i = 0, i < 5; i++)
    {
        printf("Counting to 5...\n");
    }
}