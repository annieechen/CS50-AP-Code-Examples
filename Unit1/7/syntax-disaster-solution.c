/**
 * syntax-disaster-solution.c
 * 
 * this is the code from syntax-disaster, with the syntax errors fixed
 * 
 */    
#include <cs50.h>
#include <stdio.h>

int main(void)
{
    printf("hello, world\n");
    
    int cat = 5;
    
    while (cat == 5)
    {
        cat += 3;
    }
    
    string catSound;
    
    catSound = "meow";
    
    for (int i = 0; i < 5; i++)
    {
        printf("Counting to 5...\n");
    }
}